enum Role {
    SUPER_ADMIN
    ADMIN
    USER
}

enum UserStatus {
    ACTIVE
    BLOCKED
    DELETED
}

enum WorkspaceRole {
    OWNER
    ADMIN
    MEMBER
}

enum TaskStatus {
    TODO
    IN_PROGRESS
    IN_REVIEW
    DONE
}

enum TaskPriority {
    LOW
    MEDIUM
    HIGH
}

enum ActivityAction {
    CREATED
    UPDATED
    DELETED
    ASSIGNED
    UNASSIGNED
    STATUS_CHANGED
    PRIORITY_CHANGED
    DEADLINE_CHANGED
    MEMBER_ADDED
    MEMBER_REMOVED
    COMMENT_ADDED
    COMMENT_UPDATED
    COMMENT_DELETED
}

enum ActivityEntity {
    WORKSPACE
    PROJECT
    TASK
    COMMENT
    MEMBER
}
