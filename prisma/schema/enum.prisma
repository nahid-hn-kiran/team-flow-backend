enum Role {
    SUPER_ADMIN
    ADMIN
    USER
}

enum UserStatus {
    ACTIVE
    BLOCKED
    DELETED
}

enum WorkspaceRole {
    OWNER
    ADMIN
    MEMBER
}

enum TaskStatus {
    TODO
    IN_PROGRESS
    IN_REVIEW
    DONE
}

enum TaskPriority {
    LOW
    MEDIUM
    HIGH
}
